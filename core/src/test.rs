#![feature(more_qualified_paths)]

use super::*;
use std::collections::BTreeSet;

#[test]
fn test_serialize() {
    let project = Project::new("test_description");
    let f = || "hello";
}
